<div id="item_tiles" class="col-md-12 list-group">
  <h1 class="page-header">
    Rewards By Owner
  </h1>
  <% @rewards.each do |reward| %>
    <div class="item col-md-3">
      <div class="thumbnail">
        <%= image_tag(reward.main_image) %>
        <div class="caption">
          <h4 class="group inner list-group-item-heading"><%= reward.title %></h4>
          <div class="row paddtop-20">
            <div class="col-xs-12 col-md-6">
              <p class="lead"><%= reward.price_in_coins %> coins</p>
            </div>
            <div class="col-xs-12 col-md-6">
              <%= link_to 'Buy', reward_path(reward), class: 'btn btn-success pull-right' %>
            </div>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>
